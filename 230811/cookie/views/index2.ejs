<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  </head>
  <body>
    <% if (<% isLogin %> ) { %>
    <p><%= user %>님 환영합니다!</p>
    <button type="button" onclick="signout()">로그아웃</button><br />
    <% } %> <% else { %>
    <button type="button" onclick="location.href='/login'">로그인</button><br />
    <button type="button">회원가입</button><br />
    <% } %>
    <script>
      function signout() {
        axios({
          method: "GET",
          url: "/logout",
        }).then((res) => {
          console.log(res);
        });
      }
    </script>
  </body>
</html>
